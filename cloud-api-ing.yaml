apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fms-ing
  annotations:
    kubernetes.io/ingress.class: gce
spec:
  rules:
  - http:
      paths:
      - path: /*
        backend:
          serviceName: cloud-api-basic-svc
          servicePort: 8080
